To install - clone from github
To run the script "npm start"
Example: http://localhost:8080/parseurl.php?url=http://localhost.
You can also use web interface. Just go to http:/localhost:8080/index.html.
To run unit tests go to "tests" directory and type: "php phpunit.phar URLParserTest.php"
